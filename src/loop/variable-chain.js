/**
 * @typedef {import('./scope')} Scope
 * @typedef {import('./value')} Value
 */

class VariableChain {
  /**
   * @param {Value} receiver
   * @param {Scope} scope
   * @return {Value}
   * @throws {Error}
   */
  evaluate(receiver, scope) {
  }
}

module.exports = VariableChain;
