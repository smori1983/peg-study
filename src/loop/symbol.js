const Output = require('./output');
const Scope = require('./scope');

class Symbol {
  /**
   * @param {Scope} scope
   * @param {Output} output
   */
  evaluate(scope, output) {
  }
}

module.exports = Symbol;
